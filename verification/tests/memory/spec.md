```act
behaviour test_asset_exchange_no_conflict of TEST
interface test_asset_exchange_no_conflict(uint32 asset_id, uint64 exchange_id)
```

```act
behaviour test_asset_user_no_conflict of TEST
interface test_asset_user_no_conflict(uint32 asset_id, uint64 user_id)
```

```act
behaviour test_user_exchange_no_conflict of TEST
interface test_user_exchange_no_conflict(uint64 user_id, uint64 exchange_id)
```
